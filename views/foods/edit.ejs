<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editing <%= pantryItems.name %>
    </title>
</head>

<body>
    <%- include('../partials/_navbar') %>
        <h1>Edit <%= pantryItems.name %>
        </h1>
        <form action="'/users/<% user._id %>/foods/<%= pantryItems._id %>?method=PUT" method="POST">
            <label for="name">Food Name:</label>
            <input type="text" id="name" name="name" placeholder="<%= pantryItems.name %>" required><br><br>

            <label for="brand">Brand:</label>
            <input brand="text" id="brand" name="brand" placeholder="<%= pantryItems.brand %>" required><br><br>

            <button type="submit">Edit <%= pantryItems.name %></button>
        </form>

        <a href="/">Back to Pantry</a>
</body>

</html>