<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Welcome to the Community!</h1>

        <h2>Check out others pantries!</h2>

        <ul>
            <% user.forEach(one=> { %>
                <li>
                    <a href="/users/<%=one._id%>">
                        <%= one.username %>
                    </a>
                </li>
                <% }) %>
        </ul>

</body>

</html>